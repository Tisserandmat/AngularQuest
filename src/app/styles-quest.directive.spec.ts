import { StylesQuestDirective } from './styles-quest.directive';

describe('StylesQuestDirective', () => {
  it('should create an instance', () => {
    const directive = new StylesQuestDirective();
    expect(directive).toBeTruthy();
  });
});
